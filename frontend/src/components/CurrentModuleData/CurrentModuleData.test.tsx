describe("CurrentModuleData", () => {
  test("It should have the edit button disabled when the module is not available", () => {});

  test("It should have the edit button enabled when the module is available", () => {});

  test("It should display the edit modal after clicking the edit button", () => {});

  test("It should have go back button", () => {});

  test("It should render the module details", () => {});
});
